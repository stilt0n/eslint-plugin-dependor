import { rule as noCommaSeparatedExportDeclarations } from './no-comma-separated-export-declarations';
import { rule as noUnquotedImportOrExportInJsx } from './no-unquoted-import-or-export-in-jsx';

const rules = {
  noCommaSeparatedExportDeclarations,
  noUnquotedImportOrExportInJsx,
};

export default rules;
